# --- Secrets: never commit real keys ---
.env
**/.env
*.env
!.env.example

# --- Wren local runtime state (contains DB connection incl. credentials) ---
data/
.dist/

# --- Backups of local config (may contain keys) ---
backup_ollama_*/
backup_original/

# --- OS / editor cruft ---
.DS_Store
Thumbs.db
